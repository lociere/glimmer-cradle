{
  "name": "@glimmer-cradle/extension-template",
  "version": "0.2.6",
  "private": true,
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "build:with-deps": "pnpm --filter @glimmer-cradle/contracts run build && pnpm --filter @glimmer-cradle/extension-sdk run build && pnpm run build",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "typecheck:with-deps": "pnpm --filter @glimmer-cradle/contracts run build && pnpm --filter @glimmer-cradle/extension-sdk run build && pnpm run typecheck"
  },
  "dependencies": {
    "@glimmer-cradle/extension-sdk": "workspace:*"
  },
  "peerDependencies": {
    "zod": "^3.23.0"
  },
  "devDependencies": {
    "@types/node": "^24.13.3",
    "typescript": "^5.0.0",
    "zod": "^3.23.0"
  }
}
