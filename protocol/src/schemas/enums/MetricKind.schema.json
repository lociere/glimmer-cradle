{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "https://glimmer-cradle.local/schemas/enums/MetricKind.schema.json",
  "title": "MetricKind",
  "description": "遥测指标三类（对齐 OpenTelemetry 语义）。跨层单一事实源，Kernel 内核与 Cognition 认知核共用。counter=单调累加（调用次数/错误数）；gauge=瞬时值（情绪强度/记忆条数/队列深度）；histogram=分布（延迟/耗时/token 数）。",
  "type": "string",
  "enum": ["counter", "gauge", "histogram"]
}
