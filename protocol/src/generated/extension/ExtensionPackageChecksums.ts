/* 自动生成 — 从 ExtensionPackageChecksums.schema.json 生成，勿手动修改 */

export interface ExtensionPackageChecksums {
  schema: 'glimmer-cradle.extension-checksums';
  algorithm: 'sha256';
  files: {
    path: string;
    size: number;
    sha256: string;
  }[];
}
