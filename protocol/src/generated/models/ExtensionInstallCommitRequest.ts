/* 自动生成 — 从 ExtensionInstallCommitRequest.schema.json 生成，勿手动修改 */

export interface ExtensionInstallCommitRequest {
  request_id: string;
  transaction_id: string;
  approved_permissions: string[];
}
