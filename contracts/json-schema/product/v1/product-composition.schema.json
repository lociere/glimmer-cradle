{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://glimmer-cradle.local/contracts/product/v1/product-composition.schema.json",
  "title": "ProductComposition",
  "description": "产品发行物包含的能力组合；启用状态与用户偏好不属于该契约。",
  "type": "object",
  "additionalProperties": false,
  "required": [
    "schema_version",
    "id",
    "display_name",
    "version",
    "features"
  ],
  "properties": {
    "$schema": {
      "type": "string"
    },
    "schema_version": {
      "const": 1
    },
    "id": {
      "enum": [
        "desktop",
        "personal-server"
      ]
    },
    "display_name": {
      "type": "string",
      "minLength": 1
    },
    "version": {
      "type": "string",
      "pattern": "^[0-9]+\\.[0-9]+\\.[0-9]+(?:-[0-9A-Za-z.-]+)?(?:\\+[0-9A-Za-z.-]+)?$",
      "description": "当前产品制品的语义版本；由发布链写入，不属于用户配置。"
    },
    "features": {
      "type": "object",
      "description": "Capabilities included in this product artifact. Enablement is owned by system configuration.",
      "additionalProperties": false,
      "required": [
        "control_surface_gateway",
        "local_device_actions",
        "avatar",
        "audio",
        "extensions"
      ],
      "properties": {
        "control_surface_gateway": {
          "type": "boolean"
        },
        "local_device_actions": {
          "type": "boolean"
        },
        "avatar": {
          "type": "boolean"
        },
        "audio": {
          "type": "object",
          "additionalProperties": false,
          "required": [
            "tts",
            "asr"
          ],
          "properties": {
            "tts": {
              "type": "boolean"
            },
            "asr": {
              "type": "boolean"
            }
          }
        },
        "extensions": {
          "type": "boolean"
        }
      }
    }
  },
  "x-glimmer-owner": "Product Composition owner",
  "x-glimmer-contract-kind": "Document",
  "x-glimmer-compatibility": "0.2.3 intentionally requires the immutable product artifact version for runtime compatibility checks."
}
