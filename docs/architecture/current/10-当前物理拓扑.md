# 当前物理拓扑

> 范围：描述基准 main 中真实存在并被仓库、manifest、入口或构建链消费的结构。
> 事实依据：main `aa461792f32b3bdf8e43826c62cfcfb6345bac39`，
> tree `fd8dcf879d39959809ede969280a630271a39232`。
> 维护触发：tracked 根、workspace package、入口或部署文件变化时。

本页只描述当前事实，不把 [目标物理拓扑](../blueprint/目标物理拓扑.md) 写成已落地。
覆盖口径是 tracked 顶层根、package/workspace manifest、关键入口和主要部署链，不是全仓
逐文件抄录。

## 1. 当前仓库树

```text
/
├── .codex/                         # Codex 协作配置
├── .github/workflows/
│   └── release-personal-server.yml
├── AGENTS.md
├── package.json
├── pnpm-workspace.yaml
├── pnpm-lock.yaml
├── ruff.toml
├── docs/
├── assets/
├── configs/
├── contracts/                      # M12 Slice 1 已集成的 baseline
│   ├── package.json
│   ├── buf.yaml
│   ├── buf.gen.yaml
│   ├── toolchain.json
│   ├── proto/
│   ├── json-schema/
│   ├── generated/
│   └── tests/
├── protocol/                       # 当前 runtime consumer 仍依赖的 legacy owner
│   ├── package.json
│   ├── codegen/
│   └── src/
│       ├── schemas/
│       ├── generated/
│       └── validation/
├── core/
│   ├── kernel/
│   │   ├── package.json
│   │   ├── src/index.ts
│   │   ├── src/main.ts
│   │   ├── src/application/
│   │   ├── src/domain/
│   │   ├── src/foundation/
│   │   ├── src/host/
│   │   ├── src/infrastructure/
│   │   └── src/lifecycle/
│   ├── cognition/
│   │   ├── pyproject.toml
│   │   ├── src/glimmer_cradle/cognition/
│   │   └── tests/
│   └── avatar/unity-host/          # Avatar Core 与 Unity Host 尚未物理分离
├── engines/
│   └── audio/
│       ├── pyproject.toml
│       ├── src/glimmer_cradle/audio/main.py
│       └── tests/
├── packages/
│   └── extension-sdk/
│       ├── package.json
│       └── src/
├── products/
│   ├── desktop/
│   │   ├── package.json
│   │   └── src/main/index.ts
│   └── personal-server/
│       ├── package.json
│       └── src/index.ts
├── native/
├── templates/
│   └── extension-basic/
├── data/
├── deploy/personal-server/
│   ├── .env.example
│   ├── Caddyfile
│   ├── Dockerfile
│   ├── compose.yaml
│   ├── compose.source.yaml
│   ├── bootstrap-host.sh
│   ├── install.sh
│   ├── install-remote.sh
│   ├── install-release.sh
│   ├── deploy.sh
│   ├── package-release.sh
│   ├── container/entrypoint.mjs
│   ├── container/supervisor.mjs
│   ├── container/ops-bridge.mjs
│   └── container/ops-bridge-core.mjs
└── scripts/                        # 跨 owner 编排与领域任务目前混在 root
```

以上 Personal Server 部署叶子均由基准 Git tree 逐项核对；不存在
`deploy/personal-server/lib/host-transaction.sh` 或
`deploy/personal-server/container/ops-bridge-handoff.mjs`。

## 2. 当前关键入口

| Owner | 当前入口链 | 构建输出 |
|---|---|---|
| Repository | `package.json` → `scripts/check-architecture.mjs` | 直接执行 tracked source |
| Kernel | `core/kernel/package.json` → `core/kernel/src/index.ts` → `core/kernel/src/main.ts` | `core/kernel/dist/index.js`，derived、未 tracked、基准未物化 |
| Cognition | `core/cognition/pyproject.toml` → `core/cognition/src/glimmer_cradle/cognition/host/process.py` | Python console entry，无独立 generated output |
| Desktop | `products/desktop/package.json` → `products/desktop/src/main/index.ts` | `products/desktop/dist/main/index.js`，derived、未 tracked、基准未物化 |
| Personal Server | `products/personal-server/package.json` → `products/personal-server/src/index.ts` | `products/personal-server/dist/index.js`，derived、未 tracked、基准未物化 |
| Audio | `engines/audio/pyproject.toml` → `engines/audio/src/glimmer_cradle/audio/main.py` | Python console entry，无独立 generated output |

## 3. 当前 transitional 差距

- `contracts/` baseline 已存在，但 runtime 主线仍依赖 `protocol/`；不能写成迁移完成。
- Cognition、Audio 和 Unity 仍保存 legacy generated projection。
- `core/avatar/unity-host/` 同时承载 Avatar 领域与 Unity 具体 Host。
- Extension Host 仍位于 Kernel 进程承载附近，尚未形成独立 `hosts/extension-host/`。
- Kernel 的 foundation、host、infrastructure、lifecycle 仍有职责重叠。
- root `scripts/` 仍含 Avatar、Personal Server、DLQ、Experience、Telemetry 等领域逻辑。
- 当前只有 Personal Server Release workflow；PR parity 与 Desktop Release 尚未形成。
- 当前安装、数据、缓存、日志与运行协调路径以现有部署/产品实现为准，尚未完全达到
  Blueprint 的分域目标。

主要迁移动作见 [物理拓扑差距与迁移地图](./11-物理拓扑差距与迁移地图.md)。
