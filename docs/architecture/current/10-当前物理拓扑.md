# 当前物理拓扑

> 范围：描述当前 fixed-state candidate 中真实存在并被 manifest、入口或构建链消费的结构。
> 事实依据：当前 tracked source、package/workspace manifest、部署与 workflow 入口。
> 维护触发：tracked 根、workspace package、入口或部署文件变化时。

本页只描述当前事实，不把 [目标物理拓扑](../blueprint/目标物理拓扑.md) 写成已落地。
覆盖口径是 tracked 顶层根、package/workspace manifest、关键入口和主要部署链，不是全仓
逐文件抄录。

## 1. 当前仓库树

```text
/
├── .codex/                         # Codex 协作配置
├── .github/workflows/
│   ├── pr.yml
│   ├── release-personal-server.yml
│   └── release-desktop.yml
├── AGENTS.md
├── package.json
├── pnpm-workspace.yaml
├── pnpm-lock.yaml
├── ruff.toml
├── docs/
├── assets/
├── configs/
├── contracts/                      # M12 Slice 1 已集成的 baseline
│   ├── package.json
│   ├── buf.yaml
│   ├── buf.gen.yaml
│   ├── toolchain.json
│   ├── proto/
│   ├── json-schema/
│   ├── generated/
│   └── tests/
├── protocol/                       # 当前 runtime consumer 仍依赖的 legacy owner
│   ├── package.json
│   ├── codegen/
│   └── src/
│       ├── schemas/
│       ├── generated/
│       └── validation/
├── core/
│   ├── kernel/
│   │   ├── package.json
│   │   ├── src/index.ts
│   │   ├── src/main.ts
│   │   ├── src/application/
│   │   ├── src/domain/
│   │   ├── src/foundation/
│   │   ├── src/host/
│   │   ├── src/infrastructure/
│   │   ├── src/lifecycle/
│   │   └── tools/{dlq.py,telemetry.py}
│   ├── cognition/
│   │   ├── pyproject.toml
│   │   ├── src/glimmer_cradle/cognition/
│   │   ├── tests/
│   │   └── tools/experience.py
│   └── avatar/
│       ├── scripts/
│       ├── tests/
│       └── unity-host/             # runtime split 仍属于 M12-5
├── hosts/
│   └── unity-avatar-host/
│       ├── scripts/
│       └── tests/
├── engines/
│   └── audio/
│       ├── pyproject.toml
│       ├── src/glimmer_cradle/audio/main.py
│       └── tests/
├── packages/
│   └── extension-sdk/
│       ├── package.json
│       ├── src/
│       ├── scripts/
│       └── tests/
├── products/
│   ├── desktop/
│   │   ├── package.json
│   │   ├── src/main/index.ts
│   │   ├── scripts/
│   │   └── tests/
│   └── personal-server/
│       ├── package.json
│       ├── src/index.ts
│       ├── scripts/
│       └── tests/
├── native/
├── templates/
│   └── extension-basic/
├── data/
├── deploy/personal-server/
│   ├── .env.example
│   ├── Caddyfile
│   ├── Dockerfile
│   ├── compose.yaml
│   ├── compose.source.yaml
│   ├── bootstrap-host.sh
│   ├── install.sh
│   ├── install-remote.sh
│   ├── install-release.sh
│   ├── deploy.sh
│   ├── package-release.sh
│   ├── container/entrypoint.mjs
│   ├── container/supervisor.mjs
│   ├── container/ops-bridge.mjs
│   ├── container/ops-bridge-core.mjs
│   ├── container/ops-bridge-handoff.mjs
│   ├── lib/host-transaction.sh
│   └── tests/
└── scripts/
    ├── check-architecture.mjs
    ├── check-no-bom.mjs
    └── launch-product.mjs
```

工程生命周期的精确叶子和 derived 制品位置见
[M13 完成态物理目录](../../roadmap/manifests/M13-目标物理清单.md)。

## 2. 当前关键入口

| Owner | 当前入口链 | 构建输出 |
|---|---|---|
| Repository | `package.json` → 三项 root thin orchestration | 检查、产品启动和 owner-local task graph |
| Kernel | `core/kernel/package.json` → `core/kernel/src/index.ts` → `core/kernel/src/main.ts` | `core/kernel/dist/index.js`，derived、未 tracked、基准未物化 |
| Cognition | `core/cognition/pyproject.toml` → `core/cognition/src/glimmer_cradle/cognition/host/process.py` | Python console entry，无独立 generated output |
| Desktop | `products/desktop/package.json` → source/build/package/verify | `products/desktop/dist/` 与 `dist/desktop/<version>/windows-x64/`，derived |
| Personal Server | `products/personal-server/package.json` → source/build/release/verify | `products/personal-server/dist/` 与 `dist/personal-server/<version>/linux-amd64/`，derived |
| Audio | `engines/audio/pyproject.toml` → `engines/audio/src/glimmer_cradle/audio/main.py` | Python console entry，无独立 generated output |

## 3. 当前 transitional 差距

- `contracts/` baseline 已存在，但 runtime 主线仍依赖 `protocol/`；不能写成迁移完成。
- Cognition、Audio 和 Unity 仍保存 legacy generated projection。
- `core/avatar/unity-host/` 同时承载 Avatar 领域与 Unity 具体 Host。
- Extension Host 仍位于 Kernel 进程承载附近，尚未形成独立 `hosts/extension-host/`。
- Kernel 的 foundation、host、infrastructure、lifecycle 仍有职责重叠。
- Unity runtime project 仍在 `core/avatar/unity-host/`；M13 只迁移了工程 task，M12-5
  的独立 Avatar Core/Host split 尚未实施。
- Extension Host 与 M12 runtime 分层仍未完成；M13 没有借 task graph 重排宣称完成。
- Desktop installer 已有 Windows fixed-artifact 任务与 workflow，但 clean Windows
  installer/首启/升级/卸载、签名和公证仍未实机验证。

主要迁移动作见 [物理拓扑差距与迁移地图](./11-物理拓扑差距与迁移地图.md)。
