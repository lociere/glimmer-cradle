# Legacy current architecture

本目录保留 2026-06 文档体系重构前的架构快照，供历史阶段材料和旧链接追溯。它包含混合的当前事实、目标设计、路线图和检查清单，**不再是当前架构的权威入口，也不得继续编辑**。

新工作请从 [../README.md](../README.md) 进入：当前结构在 `architecture/`，精确事实在 `reference/`，操作步骤在 `guides/`，未完成计划在 `roadmap/`。

待所有历史引用完成转向后，本目录会整体归档到 `docs/history/`。
