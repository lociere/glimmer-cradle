# M12：契约脊柱与跨进程服务架构重建

- 状态：planned
- 关联架构：[Glimmer Cradle 架构蓝图](../../architecture/blueprint/微光摇篮架构蓝图.md)
- 关联决策：[ADR-0013 契约脊柱与跨进程服务架构](../../architecture/decisions/ADR-0013-契约脊柱与跨进程服务架构.md)、[ADR-0014 仓库物理分层与器官模块边界](../../architecture/decisions/ADR-0014-仓库物理分层与器官模块边界.md)、[ADR-0009 本地监督树与动态端点治理](../../architecture/decisions/ADR-0009-本地监督树与动态端点治理.md)
- 前置里程碑：[M11：Personal Server 控制面、区域分发与跨产品 Extension 闭环](./M11-Personal%20Server控制面、区域分发与跨产品Extension闭环.md)

## 目录

- [目标成果](#目标成果)
- [范围](#范围)
- [非范围](#非范围)
- [依赖](#依赖)
- [实施阶段与旧入口删除条件](#实施阶段与旧入口删除条件)
- [成功、失败、降级与回退](#成功失败降级与回退)
- [风险](#风险)
- [验收门](#验收门)
- [完成后的归档位置](#完成后的归档位置)

## 目标成果

在 M11 完成并退出唯一活跃推进面后，把当前万能 `protocol/` 重建为 `contracts/` 契约脊柱：跨进程可调用能力使用版本化 Protobuf Service，核心器官默认通过受 Kernel 监督的 gRPC 协作，Web/Desktop Surface 只访问 Kernel Surface Gateway，配置与 package 等文档型契约继续由 JSON Schema 拥有。

完成后，TypeScript、Python 与 C# 边界拥有同一份可生成、可兼容检查的 Service 定义；Command、Query、Event、Stream、Document 语义分离；错误、deadline、cancellation、trace、causation/correlation 和 readiness 可验证；ZMQ 自制 RPC、stdio 命令协议和被替代的手写 WebSocket 主线已按边界删除，不存在无退出条件双轨。

同一里程碑还把目标物理职责落到真实仓库：`core/`、`engines/`、`hosts/`、`products/`、`packages/`、`contracts/`、`native/` 边界可由 imports、composition root、构建、测试和打包证明；Kernel 与 Cognition 收口为各自模块化单体；Avatar 领域与 UnityAvatarHost 分离；Extension Host 成为 Kernel 监督的独立不可信代码崩溃域。当前目录在迁移完成前仍由 Current/Implementation 如实描述。

## 范围

- 建立 `contracts/proto/`、`contracts/json-schema/`、Buf 配置、兼容基线和可复现生成链。
- 固定 `common`、Kernel、Cognition、Surface、Avatar、Audio Engine、Extension Host 的 Service owner、依赖方向和 `v1` package。
- 建立领域模型 ↔ generated DTO 的边界 Adapter；禁止业务层依赖生成 stub。
- 迁移 Kernel ↔ Cognition、Kernel ↔ Engine/Avatar/Extension Host 和 Surface ↔ Kernel Gateway 主线。
- 建立 typed error、deadline、cancellation、metadata、trace、causation/correlation、readiness 和 generation 校验。
- 建立 control/data plane；为媒体与 Blob 固定 stream/reference/lease 策略和阈值。
- 建立 Buf breaking、JSON Schema compatibility、TS/Python/C# round-trip、Adapter contract test 和受影响 E2E 门。
- 建立目标根职责树与依赖方向门；新增 `hosts/` 只承载具体进程 Host，不把领域实现或产品页面搬入其中。
- 将 Kernel 收口为 `domain/application/ports/adapters/runtime/composition`，删除泛化 `foundation/`、无 owner 的 `application/services/` 和 transport 对业务层的侵入。
- 将 Cognition 收口为 `domain/application/ports/adapters/host`，保持单一心智进程与一致性边界，删除领域/技术平级旧入口和心智包内部的 `protocol/generated/`。
- 建立真正的 `core/avatar/` 领域，并将 UnityAvatarHost 具体承载迁到 `hosts/`；将执行第三方代码的 Extension Host 从 Kernel 内迁到 `hosts/` 独立进程。
- 为同进程 Application 事务与跨进程 Saga/Process Manager 建立边界、幂等、补偿、causation/correlation 和失败终态测试。
- 对 `packages/` 建立公开/跨产品提取门；不预建 UI shared package，产品专属页面继续归各 Product owner。
- 在每个迁移切片通过验收后删除对应旧入口、旧生成链、兼容桥和 feature flag；最终删除 `protocol/`。
- 同步 Current、Implementation、Reference、Guide、SDK 与打包事实源，使目标物理形态和真实代码一致。

## 非范围

- 不改变 Cognition、Kernel、Avatar、Engine 或 Extension 的领域主权。
- 不把远程公网 API、托管云、多租户或第三方开放 API 混入本地器官 Service。
- 不用 Protobuf 取代配置、Character Package、Extension manifest/package 或动态 Skill/tool 参数的 JSON Schema。
- 不让 Surface 直连 Cognition、Engine、Avatar 或 Extension 内部服务。
- 不因迁移重写业务功能、人格、记忆、Avatar 行为或模型算法。
- 不把 Cognition 的 identity/persona/affect/experience/memory/conversation/context/deliberation/volition 拆成网络服务或独立一致性边界。
- 不把 Kernel 拆成微服务，不把第三方扩展业务逻辑迁入 Kernel Domain/Application。
- 不引入跨器官分布式数据库事务，也不用万能 Event Bus 或 generated DTO 代替明确的本地 use case 与跨进程 Saga。
- 不为预期复用创建 UI shared package；没有两个真实消费者、独立 API/版本/测试 owner 时不提取到 `packages/`。
- 不在本里程碑之外重排 configs/assets/data/deploy/scripts/docs 等已有明确职责根。
- 不长期保留 ZMQ/gRPC、stdio/gRPC 或手写 WebSocket/Connect 两条等价主线。
- 不在无实测证据时固定 shared memory、Connect streaming 或平台认证的具体实现。

## 依赖

- M11 完成，当前产品控制面、Extension 与发布事实稳定，避免两项跨层迁移同时争夺同一边界。
- ADR-0009 的动态端点 catalog、监督树、进程树回收和 readiness 模型保持有效。
- 受支持 Node/Python/C# 工具链能在 Windows/Linux 生成、构建并运行最小 gRPC/Connect contract test。
- 现有 Schema、producer、consumer、ZMQ/stdio/WebSocket 路径和测试清单完整，可逐边界证明删除。
- 当前根目录、package、imports、composition root、生成物、配置、测试、构建和打包消费者清单完整，可为每个旧路径建立引用归零基线。
- 发布与安装链能携带新增生成物和本地服务依赖，并保持可复现与离线安装。
- canonical `.proto`/JSON Schema、固定版本生成器与兼容基线均可从仓库和固定制品取得；Buf Schema Registry、远程插件或其他专有云服务不作为前置条件。

## 实施阶段与旧入口删除条件

### 1. 工具链与兼容基线

建立最小三语言 Service、JSON Schema dialect/fixture、Buf lint/breaking 和 round-trip。只有 Node/Python/C# 在目标平台通过生成与调用、包体/启动开销有基线后，才选择插件并进入生产迁移；失败时记录阻断并通过新 ADR 调整方向，不静默复用旧架构。

### 2. 物理基线与依赖门

为目标根职责树建立 package/import/composition/build/package 边界检查，并为每个待迁移目录记录当前消费者、owner、回退窗口和删除条件。根级 `hosts/` 只接收需要与领域 owner 分离、独立构建/分发或隔离不可信代码的具体 Host；Cognition 自身唯一进程入口仍归 `core/cognition/host/`。`hosts/` leaf 必须依据实际 package/进程身份和命名规范确定；不得先创建空壳、复制领域代码或建立永久 re-export。此阶段 Current/Implementation 继续描述现状。

### 3. Kernel ↔ Cognition 与模块化单体收口

迁移 perception、cycle/action、synthesis、heartbeat、shutdown 和 readiness，同时收口两个器官内部结构：

- Kernel 将能力迁入 `domain/application/ports/adapters/runtime/composition` 的明确 owner；当 imports、配置、测试、构建和打包引用归零后，删除旧 `foundation/`、泛化 `application/services/`、被替代的 infrastructure/host 聚合和兼容导出；
- Cognition 将心智领域模块归入 `domain/`，use case/事务归入 `application/`，技术实现归入 `adapters/`，进程入口归入 `host/`；当生成 DTO 只剩 contract Adapter 消费后，删除旧平级技术目录与 `protocol/generated/`；
- 同一进程内跨领域事务通过本地 use case、明确事务边界和领域事件完成；跨 Kernel/Cognition 进程流程才使用显式 Saga/Process Manager，不增加心智网络服务。

成功、领域失败、deadline、取消、trace、进程重启、generation mismatch、Saga 幂等/补偿与恢复全部通过后，删除 ZMQ 自制 RPC、对应万能 envelope、旧配置和旧测试入口。

### 4. Engine、Avatar 与 Extension Host

按器官逐一迁移，保持 Kernel 单一拓扑 owner：

- 建立 `core/avatar/` 对 Avatar Package/State/Behavior/Ports 的真实所有权；把 Unity/Cubism、进程 bootstrap、Host Adapter 与 framework 生命周期迁到 `hosts/` 下职责明确的 UnityAvatarHost leaf。程序集、imports、composition root、生成、配置、测试、构建和打包全部切换后，删除 `core/avatar/unity-host/` 与旧构建入口；
- 把执行第三方代码的 Extension Host 迁到 `hosts/` 独立进程；Kernel 仅保留监督、权限、catalog、调用编排与 Projection。隔离、权限拒绝、崩溃、重启、停用撤销和进程树回收通过，且消费者归零后，删除 Kernel 内旧第三方代码执行入口、旧 Worker 协议和兼容桥；
- Engine 继续归 `engines/`，只迁移跨进程 Adapter/Service，不借机改写模型与媒体业务。

每条链路在资源未就绪、崩溃、重启、降级、主动停机、流中断和进程树回收通过后，删除对应 stdio 命令协议或手写 WebSocket 主线；未通过的边界不得宣告完成。

### 5. Surface Gateway 与产品边界

Desktop/Web 只通过 Kernel Surface Gateway 读取 Projection 和提交受控 intent。Connect unary/streaming 与 WebSocket 的最终分工以浏览器兼容、背压、取消、认证和实测延迟决定。目标链路通过 deep-link 会话恢复、断线重连、权限拒绝、状态矩阵和关键交互回归后，删除被替代的手写 Surface WebSocket 主线。

Desktop 与 Personal Server 页面/流程继续归各自 Product owner。只有至少两个真实产品对 design primitives 或 surface client 形成稳定复用，并具备独立 API、兼容版本、测试和 owner 后，才提取到 `packages/`；否则不创建 UI shared package。

### 6. 数据面与最终物理收口

按实测阈值为 Audio、Avatar、图像、模型和 Blob 固定 streaming、lease/reference 或 shared memory；验证 expiry、取消、清理、背压和进程崩溃。所有包、脚本、配置、生成物、测试和文档停止引用 `protocol/` 后删除旧目录、迁移桥和 feature flag。

最终对 `core/`、`engines/`、`hosts/`、`products/`、`packages/`、`contracts/`、`native/` 做依赖、装配、生成、构建、安装和发布扫描。旧路径引用、re-export、兼容包、临时 feature flag 与迁移指标必须归零；Current/Implementation/Reference 再从“当前旧结构”更新为已经通过验证的新事实。

## 成功、失败、降级与回退

| 情况 | 语义 |
|---|---|
| unary 成功 | 方法承诺已经成立；异步工作只返回 accepted/pending 并提供状态查询 |
| typed 业务失败 | gRPC status + 稳定 detail；调用方按 code 处理，不解析 message |
| deadline / cancellation | 传播到上游并停止工作；已提交副作用按方法幂等/补偿契约解释 |
| required service 未 ready | Kernel 阻止对应 Ingress，不排入黑洞 |
| optional organ degraded | 撤销能力并更新 Projection；不伪装完整 ready |
| stream/data reference 失败 | 控制面显示失败/过期/取消；清理 lease 和临时资源 |
| 新链路切换失败 | 只在该切片明确回退窗口内恢复旧主线并保留诊断；修复后重新过门 |
| 平台级阻断 | 停止切片，由新 ADR 修改已接受方向；不得永久停在双轨 |
| 同进程事务失败 | 由本地事务边界回滚或进入明确失败终态；不发送伪成功跨域事件 |
| 跨进程 Saga 部分成功 | Process Manager 记录步骤，按契约补偿或进入可查询人工恢复态；不宣称分布式原子成功 |

## 风险

| 风险 | 应对 |
|---|---|
| Protobuf DTO 污染领域层 | import 边界检查、Adapter contract test、代码审查门 |
| gRPC/Connect 语言插件能力不齐 | 先做三语言/双平台 spike，以固定版本和最小功能矩阵选型 |
| 本地端点被其他进程冒用 | 延续动态 catalog，并验证 owner PID、进程世代与平台认证 |
| Surface Gateway 变成万能业务层 | 只做 BFF/Projection 与受控 command，领域判断返回 owner |
| stream 缺少背压或取消导致内存增长 | 有界队列、flow-control、deadline、取消和长运行指标 |
| 媒体全部走普通 RPC 导致复制和延迟 | control/data plane 分离，基于大小/频率基线决定通道 |
| 双轨迁移长期化 | 每切片先写删除对象、owner、指标和完成门；未删除不得关闭切片 |
| JSON Schema 与 Protobuf 复制同一结构 | 契约归属清单、生成/引用规则和重复定义扫描 |
| 版本号跟随应用版本失控 | `glimmer.*.v1/v2` 与产品 SemVer 分离，Buf breaking 固定基线 |
| 迁移破坏 readiness 或监督权 | ADR-0009 contract test、崩溃/重启/停机矩阵和 Kernel 拓扑断言 |
| 生成与兼容门依赖托管服务 | Schema 全量入库、工具和插件版本固定、离线生成/`buf breaking` 门、供应链许可证与摘要清单 |
| transport/runtime 渗入领域模型 | Domain/Application/Port import 门；替换 gRPC/Connect 的 Adapter contract test 不允许修改领域模型 |
| Cognition 被目录分域误拆成微服务 | 进程/端点/数据 owner 断言；心智模块只通过本地 Application/Port 协作 |
| 物理迁移只改目录外壳 | imports、composition、生成、配置、测试、构建、打包和文档联合删除门 |
| Avatar 领域继续依附 Unity | `core/avatar/` 领域 owner 测试与 Unity Host 反向依赖扫描；旧目录删除才关闭切片 |
| Extension 第三方代码回流 Kernel | 进程隔离与 import/装载扫描；Kernel 只保留监督、权限、catalog、编排和 Projection |
| `packages/` 或 UI shared 过早抽象 | 两个真实消费者及独立 API/版本/测试 owner 评审门 |

## 验收门

### 架构与物理形态

- `contracts/` 是唯一契约脊柱；Protobuf Service 与 JSON Schema 文档契约职责分明。
- `protocol/`、被替代的 ZMQ/stdio/手写 WebSocket 主线、旧配置、旧生成物和无期限桥接已删除。
- Kernel 是内部服务拓扑和 Surface Gateway owner；不存在器官横向发现/直连或 Surface 直连内部器官。
- 生成 DTO 只存在于 Adapter/transport 边界，领域层 import 门通过。
- gRPC/Connect 只出现在 Adapter/Transport/composition root；以测试替换默认 transport 时不修改领域模型与业务 Port。
- 根职责树由真实目录、package、imports、composition root、生成、配置、测试、构建和打包共同证明；`hosts/` 只承载具体进程 Host。
- Kernel 物理收口为 `domain/application/ports/adapters/runtime/composition`；旧 `foundation/`、泛化 `application/services/` 及被替代兼容入口已删除。
- Cognition 仍是一个独立进程和单一一致性边界，并收口为 `domain/application/ports/adapters/host`；心智内部不存在网络服务或 `protocol/generated/` 领域模型。
- `core/avatar/` 拥有 Avatar Package/State/Behavior/Ports；UnityAvatarHost 位于 `hosts/`，`core/avatar/unity-host/` 与旧构建入口已删除。
- Extension Host 位于 `hosts/` 独立进程；Kernel 不执行第三方 handler，且旧执行入口/Worker 协议/兼容桥已删除。
- `packages/` 中每个包都有公开或跨产品消费者、独立 API/版本/测试 owner；不存在无真实复用依据的 UI shared package。

### 兼容与跨语言

- Buf lint/breaking 对固定基线通过；破坏性变更只通过新世代和迁移计划进入。
- JSON Schema dialect、fixture 与 compatibility 门通过；同一消息无双事实源。
- TypeScript、Python、C# serialization round-trip 和所有 Adapter contract test 通过。
- 应用版本升级与 `v1/v2` 契约世代可以独立演进。
- 固定版本的 Buf/`protoc`/语言插件、JSON Schema validator 和生成链可离线复现；不访问 BSR 或其他专有云也能完成 lint、breaking、generate 和 round-trip。
- canonical `.proto`/JSON Schema 均在仓库，依赖许可证与摘要可审计；远程插件和托管 SDK 不是唯一来源。

### 生命周期与语义

- Command、Query、Event、Stream、Document 均有代表性端到端链路；无 `payload:any` 万能 envelope。
- 成功、typed failure、deadline、cancellation、trace、causation/correlation、readiness、generation mismatch 和幂等/补偿语义均有测试。
- Required/optional service 的 waiting/degraded/failed/restart/stop 投影与 Ingress 行为通过故障注入。
- ADR-0009 的动态回环端点、进程隔离、监督回收和真实 readiness 在 Windows/Linux 通过。
- Cognition/Kernel 同进程 use case 与事务回滚、跨进程 Saga/Process Manager 的幂等、补偿、超时、causation/correlation、查询和人工恢复语义均有故障注入证据。

### Surface 与数据面

- Desktop/Web 只访问 Kernel Surface Gateway；浏览器认证、权限拒绝、断线、重连、背压、取消和状态矩阵通过。
- Audio/Avatar/Blob 数据面有实测阈值、资源上限、lease/stream 清理和长运行证据；大型数据不进入普通消息。
- 性能基线覆盖启动、首次调用、稳态延迟、吞吐、内存、CPU、包体和多实例。

### 工程与文档

- 受影响 typecheck、build、Python/C# 测试、跨进程集成、Desktop/Personal Server UI、安装和停机矩阵全部通过。
- Current、Implementation、Reference、Guide、SDK、打包和排障文档与代码一致；不再把目标写成未来，也不保留旧术语主入口。
- 迁移期观测指标归零，所有 feature flag、compatibility bridge、临时端点和测试进程已清理。

## 完成后的归档位置

- 长期不变量保留在 Blueprint、ADR-0013 与 ADR-0014。
- 当前目录、Service、Adapter、生成与运行拓扑进入 `architecture/current/` 和 `architecture/implementation/`。
- 精确 Protobuf/JSON Schema、版本、错误、metadata、兼容和数据面规则进入 `reference/`。
- 开发、迁移、排障和发布操作进入 `guides/`。
- 性能基线、平台 spike、迁移切片和旧协议删除证据进入 `history/`。
- 本里程碑从 `now.md` 移除并保留完成摘要，后续里程碑成为唯一 active。
