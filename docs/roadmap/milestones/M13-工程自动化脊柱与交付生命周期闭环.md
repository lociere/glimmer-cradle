# M13：工程自动化脊柱与交付生命周期闭环

- 状态：fixed-state candidate（A～F 已实现，待独立复审与集成）
- Owner：工程生命周期总控；各 slice 由对应 product/host/data owner 持有原子任务
- 关联架构：[架构蓝图](../../architecture/blueprint/微光摇篮架构蓝图.md)
- 关联决策：[ADR-0015](../../architecture/decisions/ADR-0015-工程自动化平面与交付生命周期分层.md)
- 精确物理完成态：[M13 完成态物理目录](../manifests/M13-目标物理清单.md)

> 本页记录候选 fixed state 的成果、边界和复审门。它不表示该提交已经集成到 main，也不把
> 未运行的真实 Docker、Ubuntu、Windows installer、签名、公证或生产发布写成验证事实。

## 目标成果

```text
fixed source
  -> owner-local atomic task
  -> root thin orchestration
  -> local / PR CI
  -> fixed artifact
  -> install / ops transaction owner
```

公共任务必须可说明 `inputs`、`outputs`、`side effects`、`lock`、`idempotency`、
`confirm`、`dry-run`、稳定 `exit code`、脱敏诊断和 `cleanup/recovery`。Release 只消费
前序固定制品；查询不取得写锁；高风险动作缺确认时失败闭合。

## 范围与保护边界

- 范围：部署事务、备份/Smoke/DLQ、task graph/CI、owner-local tooling、Personal Server
  供应链、Desktop Windows packaging。
- 保护：M12 Contract Spine 与 runtime consumer 迁移、业务协议、人物/认知/Avatar 行为、
  UI、真实服务器/数据库/Docker daemon、Registry、Release、签名和公证。
- `contracts/` 与 `protocol/` canonical 内容及生成物未因 M13 改写。
- Unity runtime 仍位于 `core/avatar/unity-host/`；它到 `hosts/unity-avatar-host/` 的 runtime
  split 继续属于 M12 Slice 5。M13 D 只完成工程任务 owner-local 收口。

## Slice A：部署事务安全

- 唯一 owner：`deploy/personal-server/lib/host-transaction.sh`。
- 所有宿主写入口共享 `/run/glimmer-cradle/host.lock`；guard inode、FD、owner、mode、
  canonical path 和安全祖先共同防止伪造、symlink 与锁文件替换，PID 只用于诊断。
- `status/logs` 在 version、Docker elevation、token/env/state/权限初始化之前分流，只用当前
  用户权限查询 Docker。
- Ops Bridge 先启动 one-shot 外部 owner、等待其持锁 ready，再返回 accepted；HTTP 响应后
  ack，旧 bridge 此后才允许被回收。
- backup/restore 补偿不能恢复操作前服务或数据时写入 `recovery_required`、精确
  `recovery_action` 并返回 78。
- installer 原样传播 deploy 的 70/75/78/130/143；只有外层补偿失败才升级 78。
- 执行阶段名为 `commit`，终态名为 `committed`。

## Slice B：数据与恢复安全

- `manual`、`transaction`、`restore-safety` 备份分根；history retention 只清理 transaction。
- Smoke 用 `fs.cp` 创建真实临时副本，拒绝 symlink/junction，退出后删除临时根。
- DLQ replay 把 payload 交给显式 dispatcher，只有 receipt 为 accepted/success 且带
  `receipt_id` 才标记 replayed；失败保留证据。
- resolve 需要确认且不产生 replay 标记；cleanup 只删除超过 retention 的 replay 成功记录。
- `show` 默认只输出脱敏摘要；raw payload/stack 需要 root 和 `--confirm`。

## Slice C：Task graph 与 CI parity

- package 重复 protocol prehook 已删除；root build 按依赖图只构建一次。
- `pnpm test` 不再隐含 Bash；Linux release fixture 使用显式 `:linux` 任务。
- `.github/workflows/pr.yml` 在 Windows/Linux 调用同一个 `pnpm check:pr`。
- encoding inventory 覆盖 contracts、deploy、workspace、ignore 与受支持文本类型。
- `prepare-runtime.mjs` 记录每个 output 的 path/size/SHA-256 与总 digest；输入或输出不匹配
  才重建，不接受单 sentinel。

## Slice D：Owner-local 工具收口

- Avatar helper/build test 归 `core/avatar/`；Unity 工程任务归
  `hosts/unity-avatar-host/`；Extension SDK staging 归 `packages/extension-sdk/`。
- Personal Server release/install/smoke/version 任务归 `products/personal-server/`；DLQ、
  telemetry、experience 分别归 Kernel/Cognition owner。
- root `scripts/` 只保留 `check-architecture.mjs`、`check-no-bom.mjs`、
  `launch-product.mjs`，旧 root 实现与 `scripts/lib/` 已删除。

## Slice E：可复现供应链

- Dockerfile base image 只接受 `@sha256:` 固定输入；CI 在构建前验证三项引用。
- tar/OCI 固定目录生成 `artifact-manifest.json`、`provenance.json`、`sbom.spdx.json`、
  `attestation.json`，独立 verifier 重新计算 digest。
- build job 权限为 contents read/packages write；release job 只有 contents write，且只下载
  前序 artifact 后发布，不现场构建。
- 现有 PS checksum、OCI identity、原子安装与回滚链继续保留。

## Slice F：Desktop packaging

- `products/desktop/scripts/package.mjs` 是 Windows x64、publish=false 的产品入口；
  `electron-builder` 精确锁定。
- 输出固定为 `dist/desktop/<version>/windows-x64/`，包含 installer、manifest、
  provenance、SBOM 与 attestation；verifier 独立重算绑定。
- `.github/workflows/release-desktop.yml` 与 Personal Server workflow 完全独立，只上传固定
  artifact，不创建外部 Release。
- 当前未声明签名、公证、正式发布或其他平台支持；新增平台必须新建独立验证矩阵。

## 删除门

- `rg --files scripts` 必须只得到三项 root 编排。
- `deploy/personal-server/host-transaction.sh`、旧 root 领域脚本、旧 install fixture 与
  `scripts/lib/` 不得存在。
- 写部署不得绕过 canonical transaction owner；Bridge 不得直接执行完整自替换事务。
- Workflow 不复制原子任务；Release job 不执行 build/package。
- Personal Server 与 Desktop 不共用 workflow、制品目录或 publish 状态机。

## Fixed-state 验证门

候选提交必须记录并通过：

1. Shell/Node/Python 受影响单测与 fake-host 故障注入；
2. Personal Server transaction/release/remote-install 与产品 test/typecheck；
3. owner-local task、architecture、encoding、version、changed Markdown links、diff check；
4. `pnpm typecheck`、`pnpm build`；
5. Desktop dry-run 与 fixed-artifact manifest/verifier；
6. 未运行的真实 Ubuntu Docker、Windows installer、签名、公证、Registry 和生产门明确记为
   remaining risk，不能外推为通过。

## 验证账本

账本代码状态为本文件所在的候选 commit；独立复审以该 commit/tree 为唯一输入。

| 验证 | 结果 |
| --- | --- |
| `node --test` owner-local tooling、deploy、Desktop、Personal Server fixture | 25/25 通过 |
| Personal Server 产品测试 | 26/26 通过 |
| Kernel DLQ `unittest` | 8/8 通过 |
| Kernel Vitest | 31 files、106/106 通过 |
| fake-host transaction、release installer、remote installer（WSL） | 通过；未调用真实 Docker |
| 受影响 Shell/Node 语法检查 | 通过 |
| Runtime prepare output manifest | 通过；四项任务均记录 input/output digest、path、size、SHA-256 |
| Personal Server、Desktop 与全仓 TypeScript typecheck | 通过 |
| 全仓 build | 通过；首次受沙箱祖先目录读取限制失败，按同一代码在本地构建权限下重跑通过 |
| architecture、encoding、version、changed Markdown links、diff check | 通过 |

未运行：真实 Ubuntu/Docker/sudo 部署、Registry、GitHub Release、Desktop installer 实际构建与
安装、签名、公证和生产操作；本机 Cognition 完整 pytest 因 Python 依赖不可用且受限网络无法
补齐而未运行，DLQ 改动以 stdlib `unittest` 覆盖。上述项目保留为独立复审或对应实机阶段风险。

## 下一门

唯一下一步是对候选 commit 做独立复审；复审应重点检查 lock trusted namespace、handoff
生命周期、补偿失败终态、旧入口扫描、DLQ cleanup、Release job 分层、Desktop clean Windows
runner 以及实际 diff 与物理清单一致性。复审通过后由总控集成；在此之前状态保持
`fixed-state candidate`。
