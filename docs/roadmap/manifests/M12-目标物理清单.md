# M12 完成态物理目录

> Target 上游：[目标物理拓扑](../../architecture/blueprint/目标物理拓扑.md)。
> 状态基准：M12 Slice 2 clean commit。
> 维护触发：M12 slice 的最终路径、owner、迁移动作或删除门变化时。

Slice 1 与 Slice 2 已集成；Slice 3～9 尚未执行。以下目录树是每个 slice
完成后的精确落点；只有标记“已完成”的切片表示当前事实。

## M12-1 Contract Spine baseline（已完成）

```text
contracts/
├── package.json
├── buf.yaml
├── buf.gen.yaml
├── toolchain.json
├── proto/glimmer/common/v1/
├── json-schema/skill/v1/
├── compatibility/
├── generated/
└── tests/
```

- 动作：retain 已集成的 `contracts/` baseline。
- 删除门：无；本 slice 不删除 `protocol/`，也不迁移 runtime consumer。

## M12-2 Kernel–Cognition Service（已完成）

```text
contracts/proto/glimmer/
├── common/v1/
├── kernel/v1/
└── cognition/v1/

core/kernel/src/adapters/cognition/
core/cognition/src/glimmer_cradle/cognition/adapters/kernel/
contracts/generated/ts/glimmer/cognition/v1/
contracts/generated/python/glimmer/cognition/v1/
```

- 动作：把 Kernel↔Cognition 的 ZMQ/envelope 契约从 `protocol/src/schemas/` 迁入
  `contracts/proto/glimmer/kernel/v1/` 与 `contracts/proto/glimmer/cognition/v1/`。
- 删除门：旧 transport、envelope、generated import、validator、fixture 和 package
  dependency consumer 归零；deadline、取消、错误、trace、readiness 通过。
- 完成证据：`contracts:verify`、TS/Python adapter contract test、双代真实启停/重启、全仓旧引用扫描。
- 安装证据：`contracts/pyproject.toml` 是 Python DTO distribution；Desktop 使用
  `products/desktop/runtime-python/uv.lock` 聚合安装态依赖，Personal Server OCI builder 显式
  构建/安装 Contracts，不依赖源码树 `PYTHONPATH`；Desktop Kernel dependencies 使用 hoisted
  实体投影，并在 staging 重命名后的最终树隔离加载 Contracts TS Service，禁止悬空 junction。
- 生命周期证据：FD 3 challenge 绑定 generation/endpoint/监督树；感知 operation 覆盖真实
  Cycle/推理取消与终态，operation id/trace 冲突会拒绝；反向 action 由 Kernel 持有
  deadline/cancellation，合成共享取消信号，稳定 invocation id 与步骤账本覆盖 post-commit 重试，
  终态不明时拒绝自动重放；
  crash、重启失败、恢复与 `/readyz` 撤销/恢复均有故障注入。

## M12-3 Kernel physical layout

```text
core/kernel/
├── package.json
├── src/index.ts
├── src/main.ts
├── src/domain/
├── src/application/
├── src/ports/
├── src/adapters/
├── src/runtime/
├── src/composition/
└── tests/
    ├── domain/
    ├── application/
    ├── ports/
    ├── adapters/
    └── runtime/
```

- 动作：保留 `package.json`、`src/index.ts`、`src/main.ts`；将 foundation、host、
  infrastructure、lifecycle 的职责 split 到目标层。
- 删除门：旧 import、兼容 re-export、重复 lifecycle owner 与 Projection writer 归零。

## M12-4 Cognition layout 与 Documents

```text
core/cognition/
├── pyproject.toml
├── src/glimmer_cradle/cognition/
│   ├── domain/
│   ├── application/
│   ├── ports/
│   ├── adapters/
│   └── host/
└── tests/

contracts/json-schema/
├── character/v1/
└── config/v1/
contracts/generated/python/glimmer/
├── common/v1/
├── cognition/v1/
└── config/v1/
```

- 动作：拆分 Cognition 内部层次；把 Character、Config、Memory Document 与 Python
  projection 收口到 Contract Spine。
- 删除门：`core/cognition/.../protocol/generated/` 的 import、fixture 和手写等价模型
  归零。

## M12-5 Avatar Core 与 UnityAvatarHost

```text
core/avatar/
├── src/Domain/
├── src/Application/
├── src/Ports/
└── tests/

hosts/unity-avatar-host/
├── Assets/Scripts/GlimmerCradle/
│   ├── Adapters/
│   ├── Host/
│   ├── Infrastructure/
│   └── Editor/
├── Packages/
├── ProjectSettings/
└── tests/

contracts/generated/csharp/GlimmerCradle/avatar/v1/
```

- 动作：将 `core/avatar/unity-host/` split 为 Avatar Core 与具体 Unity Host。
- 删除门：旧 Unity 工程、程序集、Desktop/Kernel 路径引用及 legacy generated C#
  consumer 归零。

## M12-6 Extension Host 与公开 SDK

```text
hosts/extension-host/
├── package.json
├── src/main.ts
├── src/application/
├── src/ports/
├── src/adapters/
└── tests/

packages/extension-sdk/
├── package.json
├── src/
└── tests/

contracts/proto/glimmer/extension/v1/
contracts/json-schema/extension/v1/
templates/extension-basic/
├── extension-manifest.yaml
├── gcex.package.yaml
├── config/
└── src/
```

- 动作：把第三方代码承载从 Kernel 内部移动到独立 Host；保留公开 SDK 与模板入口。
- 删除门：Kernel 内 module loader、legacy extension schema、旧进程入口与依赖归零。

## M12-7 Surface Gateway 与产品

```text
contracts/proto/glimmer/surface/v1/
contracts/json-schema/presentation/v1/
core/kernel/src/adapters/surface/

products/desktop/
├── src/main/
├── src/preload/
├── src/renderer/
└── tests/

products/personal-server/
├── src/server/
├── src/web/
└── tests/
```

- 动作：Desktop 与 Personal Server consumer 移到 Kernel Surface Gateway。
- 删除门：直接器官 import、旧 preload/API、通用 IPC envelope 与被替代 WebSocket
  consumer 归零。

## M12-8 Media control/data plane

```text
contracts/proto/glimmer/
├── avatar/v1/
└── engine/audio/v1/
contracts/generated/
├── ts/glimmer/avatar/v1/
├── ts/glimmer/engine/audio/v1/
├── python/glimmer/engine/audio/v1/
└── csharp/GlimmerCradle/avatar/v1/

engines/audio/src/glimmer_cradle/audio/adapters/contracts/
hosts/unity-avatar-host/Assets/Scripts/GlimmerCradle/Adapters/
```

- 动作：把 Audio/Avatar 控制契约与生成投影迁入唯一 Contract Spine。
- 删除门：旧 stdio/envelope、手写镜像、多根 generated projection、deadline/
  cancellation/readiness consumer 归零。

## M12-9 Legacy Protocol closure

```text
protocol/    # 删除
```

- 动作：删除整个 `protocol/`；不创建替代目录。
- 前置：M12-2～8 已逐项迁移 producer、consumer、package dependency、build source、
  generated mirror、validator 与 fixture。
- 删除门：上述七类内容引用全部归零；`pnpm-lock.yaml` 中的 dependency artifact
  引用已更新，但不要求删除 lockfile 本身；`deploy/personal-server/Dockerfile` 不再
  运行 legacy protocol build。

## Milestone 完成门

- `protocol/` 不存在，`contracts/` 是唯一跨边界契约源。
- Kernel、Cognition、Avatar、Unity Host、Extension Host 和产品 Surface 均位于上述
  owner 目录。
- 生成物只由 `contracts/` 产生，业务层不依赖 generated DTO。
- Current 与 Implementation 只在对应 slice 落地后更新为当前事实。
