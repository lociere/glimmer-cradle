# M13 完成态物理目录

> Target 上游：[目标物理拓扑](../../architecture/blueprint/目标物理拓扑.md)。
> 状态：A～F 已形成 fixed-state candidate，待独立复审与集成。
> 边界：本清单只覆盖 M13 工程生命周期；M12 runtime/Contract Spine 迁移不在本次实现中。

## Slice A：部署事务安全

```text
deploy/personal-server/
├── bootstrap-host.sh
├── install.sh
├── install-remote.sh
├── install-release.sh
├── deploy.sh
├── package-release.sh
├── compose.yaml
├── container/
│   ├── ops-bridge.mjs
│   ├── ops-bridge-core.mjs
│   └── ops-bridge-handoff.mjs
├── lib/host-transaction.sh
└── tests/
    ├── deployment-contract.test.mjs
    ├── ops-bridge-core.test.mjs
    ├── test-host-transaction.sh
    ├── transaction-contract.fixture.json
    └── fixtures/
```

运行投影：

```text
/run/glimmer-cradle/
├── host.lock
├── .host.lock.guard
├── ops-bridge.sock
└── handoff/
    ├── <operation-id>.request.json
    ├── <operation-id>.result.json
    ├── <operation-id>.ack
    └── <operation-id>.decision
/var/lib/glimmer-cradle/transactions/
├── current.json
└── events.jsonl
```

Install/Ops owner 拥有唯一事务状态机；Bridge 只做外部 handoff。完成态不得存在
`deploy/personal-server/host-transaction.sh`、绕锁写入口、容器内自替换主线或第二事务 owner。

## Slice B：数据与恢复安全

```text
core/kernel/
├── src/foundation/event-bus/dlq-replay-ingress.ts
├── src/foundation/event-bus/dlq-replay-ingress.test.ts
├── src/lifecycle/runtime/dlq-replay-runtime.ts
├── tools/dlq-replay-dispatcher.mjs
├── tools/dlq.py
├── tools/telemetry.py
└── tests/tools/test_dlq.py
core/cognition/
├── tools/experience.py
└── tests/test_dlq_cli.py
products/personal-server/
├── scripts/smoke.mjs
└── tests/smoke-data-isolation.test.mjs
```

运行投影：

```text
/var/lib/glimmer-cradle/data/backups/
├── manual/
├── transaction/
└── restore-safety/
```

`manual` 不受 transaction history 清理；Smoke 只写真实复制的临时根；DLQ replay 必须获得
dispatcher receipt，resolve 不等于 replay，cleanup 只删除超过 retention 的 replay 成功记录。
完成态删除 root `scripts/{dlq.py,telemetry.py,experience.py,smoke-personal-server.mjs}`。

## Slice C：Task graph 与 CI parity

```text
package.json
pnpm-lock.yaml
pnpm-workspace.yaml
scripts/
├── check-architecture.mjs
├── check-no-bom.mjs
└── launch-product.mjs
products/desktop/scripts/
├── prepare-runtime.mjs
└── runtime-output-manifest.mjs
products/desktop/tests/runtime-output-manifest.test.mjs
packages/extension-sdk/scripts/stage-host-modules.mjs
.github/workflows/
├── pr.yml
├── release-personal-server.yml
└── release-desktop.yml
build/reports/checks/runtime-output-manifest.json    # derived
```

Root 只保留跨 owner 编排；PR 的 Windows/Linux job 调用同一个 `pnpm check:pr`。完成态不含
package 重复 protocol prehook、`pnpm test` 的隐式 Bash、单 sentinel prepare 成功或 workflow
私有重写任务。`pnpm-workspace.yaml` 的 `allowBuilds` 明确允许 Desktop 固定依赖
`electron-winstaller` 的受控架构选择脚本，clean runner 不依赖交互式 approve 状态。

## Slice D：Owner-local 工具收口

```text
core/avatar/
├── scripts/
│   ├── avatar-package-catalog.mjs
│   ├── avatar-paths.mjs
│   ├── build-core.mjs
│   └── verify-core.mjs
└── tests/
hosts/unity-avatar-host/
├── scripts/
│   ├── build.mjs
│   ├── fixed-artifact.mjs
│   ├── project-sdk.mjs
│   ├── sync-assets.mjs
│   ├── unitypackage-projector.mjs
│   └── verify.mjs
└── tests/
packages/extension-sdk/
├── scripts/
│   ├── package-manager-command.mjs
│   └── stage-host-modules.mjs
└── tests/
products/desktop/scripts/sync-assets.mjs
products/personal-server/scripts/
├── check-version.mjs
├── test-release.sh
├── test-remote-install.sh
└── verify-full-install.sh
deploy/personal-server/tests/fixtures/install-interrupt/
```

本 slice 只迁移工程任务所有权，不实施 M12 的 Avatar Core/Unity runtime split。完成态 root
`scripts/` 仅保留 C 中三项；旧 root 任务及 `scripts/lib/` 兼容实现必须不存在。

## Slice E：Personal Server 可复现供应链

```text
deploy/personal-server/
├── Dockerfile
├── package-release.sh
└── tests/
products/personal-server/
├── scripts/
│   ├── build-image.mjs
│   ├── package-release.mjs
│   ├── release-manifest.mjs
│   └── verify-release.mjs
└── tests/supply-chain-contract.test.mjs
.github/workflows/release-personal-server.yml
dist/personal-server/<version>/linux-amd64/           # derived fixed artifact
├── glimmer-cradle-personal-server-v<version>-linux-amd64.tar.gz
├── glimmer-cradle-personal-server-v<version>-linux-amd64-full.tar.gz
├── artifact-manifest.json
├── provenance.json
├── sbom.spdx.json
└── build-claim.json
```

Dockerfile 的三项 base image 都是无默认值的 digest 输入；build job 生成并上传 fixed artifact，
release job 只下载并发布。完成态不含 tag-only base、Release job 现场重建或仅 checksum 的证明链。

## Slice F：Desktop packaging 产品矩阵

```text
products/desktop/
├── electron-builder.json
├── package.json
├── scripts/
│   ├── package.mjs
│   ├── prepare-package.mjs
│   ├── prepare-package-runtimes.mjs
│   ├── release-manifest.mjs
│   └── verify-package.mjs
├── src/main/
│   ├── packaged-paths.ts
│   └── packaged-supervisor.ts
└── tests/
    ├── package-contract.test.mjs
    ├── packaged-supervisor.test.ts
    └── runtime-output-manifest.test.mjs
.github/workflows/release-desktop.yml
build/artifacts/avatar-windows-x64/                  # derived fixed artifact
├── avatar/
├── native/
└── artifact-manifest.json
build/runtime/desktop/windows-x64/                   # derived
├── node/node.exe
├── python/Scripts/python.exe
├── python/Lib/site-packages/
├── kernel/dist/
├── kernel/node_modules/
└── runtime-manifest.json
build/staging/desktop/windows-x64/resources/          # derived
├── component-manifest.json
├── runtime/
├── components/{avatar,native}/
├── products/desktop/product.json
└── extension-host/modules/
dist/desktop/<version>/windows-x64/                  # derived fixed artifact
├── GlimmerCradle-Setup.exe
├── win-unpacked/GlimmerCradle.exe
├── win-unpacked/resources/app.asar
├── win-unpacked/resources/app.asar.unpacked/dist/main/
│   ├── packaged-paths.js
│   └── packaged-supervisor.js
├── win-unpacked/resources/component-manifest.json
├── win-unpacked/resources/components/
├── win-unpacked/resources/extension-host/
├── win-unpacked/resources/runtime/
├── win-unpacked/resources/products/desktop/product.json
├── artifact-manifest.json
├── provenance.json
├── sbom.spdx.json
└── build-claim.json
```

Desktop 与 Personal Server 任务、workflow 和制品目录独立；`electron-builder` 精确锁定。
当前支持矩阵只有 Windows x64，签名、公证和外部发布不属于本 fixed state。

## 变更授权与删除门

- 允许路径：本页列出的 source/test/workflow/root manifest、对应 Current/Implementation/
  Reference/Guide/Roadmap 与 Blueprint 目标路径。
- 保护路径：`contracts/`、`protocol/` canonical 语义和生成物、M12 runtime consumer、业务 UI、
  生产数据、真实服务器、Registry 与 Release。
- 实际 diff 必须能映射到上述 A～F；发现清单外路径时先更新本页并说明 owner/原因。
- 审查同时验证目标路径已存在、旧路径已删除、root `scripts/` 只有三项，以及 derived artifact
  不进入 Git。
