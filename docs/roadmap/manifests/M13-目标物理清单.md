# M13 完成态物理目录

> Target 上游：[目标物理拓扑](../../architecture/blueprint/目标物理拓扑.md)。
> 状态：Slice A paused/non-reproducible；Slice B～F planned。
> 维护触发：M13 slice 的最终路径、owner、迁移动作或删除门变化时。

## Slice A：部署事务安全（paused）

```text
deploy/personal-server/
├── bootstrap-host.sh
├── install.sh
├── install-remote.sh
├── install-release.sh
├── deploy.sh
├── package-release.sh
├── container/ops-bridge.mjs
├── container/ops-bridge-core.mjs
├── container/ops-bridge-handoff.mjs
└── lib/host-transaction.sh

/run/glimmer-cradle/
├── host.lock
└── ops-bridge.sock
```

- retain：现有 bootstrap/install/deploy/package 与 Ops Bridge 入口。
- create：canonical `lib/host-transaction.sh` 和稳定 handoff Adapter。
- 删除门：旧绕锁路径、自替换链和重复事务状态机 consumer 归零。
- 当前暂停 checkpoint 只涉及未提交的
  `deploy/personal-server/host-transaction.sh`；它不是 Target，也不是 Current 或
  accepted fixed state。恢复时原 owner 必须在新 main 上复核并把有效内容重放到
  `deploy/personal-server/lib/host-transaction.sh`。

## Slice B：数据与恢复安全（planned）

```text
core/kernel/tools/
├── dlq.py
└── telemetry.py
core/cognition/tools/experience.py

/var/lib/glimmer-cradle/data/backups/
├── manual/
├── transaction/
└── restore-safety/
/var/cache/glimmer-cradle/
/var/log/glimmer-cradle/
```

- move：`scripts/dlq.py`、`scripts/telemetry.py`、`scripts/experience.py` 到 owner-local
  tools。
- split：备份 retention、Smoke fixture、DLQ replay/resolve/cleanup。
- 删除门：root 领域实现、混合 backup cleanup、真实数据可写 link 与伪 replay 入口归零。

## Slice C：Task graph 与 CI parity（planned）

```text
.github/workflows/
├── pr.yml
├── release-personal-server.yml
└── release-desktop.yml

build/reports/
├── checks/
├── tests/
├── contracts/
├── packages/
└── install/
```

- create：PR CI 与统一报告目录；retain 现有 repository checks。
- 完成门：owner-local task、root 薄编排、本地/CI 同构、Windows/Linux 基础门和输出
  manifest 可验证；未来可增加轻量路径检查，但本 slice 不预先固定实现文件或工具。
- 删除门：workflow 私有业务逻辑、重复 prehook 和仅靠 sentinel 成功的入口归零。

## Slice D：Owner-local 物理收口（planned）

```text
core/avatar/scripts/
├── build-core.mjs
└── verify-core.mjs
hosts/unity-avatar-host/scripts/
├── build.mjs
├── project-sdk.mjs
├── sync-assets.mjs
└── verify.mjs
products/personal-server/scripts/
├── build-image.mjs
├── package-release.mjs
├── release-manifest.mjs
├── smoke.mjs
└── verify-release.mjs
scripts/
├── check-architecture.mjs
├── check-no-bom.mjs
└── launch-product.mjs
```

- move：Avatar、Unity、Personal Server 原子任务到 owner-local 目录。
- retain：真正跨 owner 的 root 薄入口。
- 删除门：旧 root 领域实现、重复 helper、兼容 wrapper、workflow/Guide 旧调用归零。

## Slice E：Personal Server 可复现供应链（planned）

```text
deploy/personal-server/
├── .env.example
├── Caddyfile
├── Dockerfile
├── compose.yaml
├── compose.source.yaml
├── container/entrypoint.mjs
└── container/supervisor.mjs

build/staging/personal-server/linux-amd64/
dist/personal-server/<version>/linux-amd64/
├── glimmer-cradle-personal-server.tar.gz
├── image-archive.tar
├── artifact-manifest.json
└── provenance.json

/opt/glimmer-cradle/
├── current
├── releases/<version>/
├── app/
├── container/
└── default-config/
```

- retain：上述部署叶子中的供应链与容器文件；Dockerfile 的 legacy protocol build
  引用由 M12-9 删除。
- create：fixed staging、artifact manifest、provenance 和版本化安装投影。
- 删除门：tag-only base image、Release 现场重建、只有 checksum 的证明和不可追溯
  artifact 入口归零。

## Slice F：Desktop packaging 产品矩阵（planned）

```text
products/desktop/scripts/
├── dev-electron.mjs
├── package.mjs
├── release-manifest.mjs
└── verify-package.mjs

build/staging/desktop/windows-x64/
dist/desktop/<version>/windows-x64/
├── GlimmerCradle-Setup.exe
├── artifact-manifest.json
└── provenance.json

%LOCALAPPDATA%\Programs\Glimmer Cradle\
├── GlimmerCradle.exe
├── resources\app.asar
├── resources\components\
└── manifest.json

%APPDATA%\Glimmer Cradle\
├── config/
├── data/state/
├── data/models/
├── data/packages/
└── data/backups/

%LOCALAPPDATA%\Glimmer Cradle\
├── cache/
├── work/
├── logs/
└── run/
```

- create：Desktop 独立 package/release、fixed artifact、安装与数据投影。
- retain：`products/desktop/scripts/dev-electron.mjs`。
- 删除门：借用 Personal Server Release 的错误入口、临时 package 命令、重复产物定义
  和安装/数据混写路径归零。

## Milestone 完成门

- 每个 slice 的实际 diff 只修改其明确列出的 owner 路径；新增路径先更新本页。
- Release 只消费 fixed artifact；Install/Ops 共享宿主事务 owner。
- Current、Implementation、Reference 与 Guide 只在对应 slice 落地后更新。
