# 摇篮本体密钥模板。复制为 secrets.yaml 后只在本机填写；真实值不得提交。
# 环境变量优先于本文件。扩展密钥由扩展自己的配置与授权边界管理，不写入本体模板。

providers:
  dashscope:
    # Embedding 与其他 DashScope 能力可共享；DASHSCOPE_API_KEY 环境变量优先。
    api_key: ""
  qwen:
    api_key: ""
  deepseek:
    api_key: ""
  openai:
    api_key: ""

audio:
  dashscope:
    # 可由 DASHSCOPE_API_KEY 环境变量覆盖。
    api_key: ""
