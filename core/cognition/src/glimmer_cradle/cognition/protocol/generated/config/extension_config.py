# generated by datamodel-codegen:
#   filename:  ExtensionConfig.schema.json

from __future__ import annotations

from pydantic import BaseModel, ConfigDict, Field


class ExtensionSandbox(BaseModel):
    model_config = ConfigDict(
        extra='forbid',
        frozen=True,
    )
    enable_isolation: bool | None = Field(True, description='是否启用扩展隔离')
    timeout_ms: int | None = Field(
        5000, description='扩展 onActivate / onDeactivate 超时（ms）', ge=0
    )
    allow_native_modules: bool | None = Field(
        False, description='是否允许扩展加载 native 模块'
    )


class ExtensionConfig(BaseModel):
    model_config = ConfigDict(
        extra='forbid',
        frozen=True,
    )
    extension_root_dir: str | None = Field(
        'data/packages/extensions',
        description='已安装扩展包根目录（相对应用根）；源码仓库不参与运行期发现',
    )
    sandbox: ExtensionSandbox | None = Field(
        {}, description='沙箱策略', validate_default=True
    )
    default_permissions: list[str] | None = Field(
        [
            'CHAT_SEND',
            'CONFIG_READ_SELF',
            'CONFIG_WRITE_SELF',
            'CONFIG_READ_GLOBAL',
            'EVENT_SUBSCRIBE',
        ],
        description='新扩展默认获得的权限列表',
    )
    extension_blacklist: list[str] | None = Field(
        [], description='扩展黑名单（ID 列表，阻止加载）'
    )
