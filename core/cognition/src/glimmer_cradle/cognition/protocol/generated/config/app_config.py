# generated by datamodel-codegen:
#   filename:  AppConfig.schema.json

from __future__ import annotations

from pydantic import BaseModel, ConfigDict, Field


class SystemIdentityConfig(BaseModel):
    model_config = ConfigDict(
        extra='forbid',
        frozen=True,
    )
    app_name: str | None = Field('Glimmer Cradle', description='应用显示名称')
    app_version: str | None = Field('0.1.0', description='语义化版本号（仅展示）')


class ActiveCharacterConfig(BaseModel):
    model_config = ConfigDict(
        extra='forbid',
        frozen=True,
    )
    active_id: str = Field(
        ..., description='当前激活角色 profile id', pattern='^[a-z0-9][a-z0-9_-]*$'
    )
    profile_root: str = Field(
        ...,
        description='角色 profile 根目录，相对 configs/',
        pattern='^[a-z0-9](?:[a-z0-9_-]|/[a-z0-9][a-z0-9_-]*)*$',
    )


class SystemBackupConfig(BaseModel):
    model_config = ConfigDict(
        extra='forbid',
        frozen=True,
    )
    enabled: bool | None = Field(True, description='是否启用自动备份')
    backup_dir: str | None = Field(
        'data/backups', description='自动备份存放目录（相对项目根）'
    )
    interval_hours: int | None = Field(
        24, description='自动备份间隔（小时），0 = 不调度自动备份', ge=0
    )


class AppConfig(BaseModel):
    model_config = ConfigDict(
        extra='forbid',
        frozen=True,
    )
    identity: SystemIdentityConfig
    character: ActiveCharacterConfig
    backup: SystemBackupConfig
