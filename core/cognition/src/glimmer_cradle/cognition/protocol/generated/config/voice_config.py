# generated by datamodel-codegen:
#   filename:  VoiceConfig.schema.json

from __future__ import annotations

from pydantic import BaseModel, ConfigDict, Field


class VoiceProsodyConfig(BaseModel):
    model_config = ConfigDict(
        extra='forbid',
        frozen=True,
    )
    rate: float | None = Field(1, ge=0.5, le=2.0)
    pitch: float | None = Field(1, ge=0.5, le=2.0)
    volume: int | None = Field(50, ge=0, le=100)


class CloudVoiceBinding(BaseModel):
    model_config = ConfigDict(
        extra='forbid',
        frozen=True,
    )
    voice_id: str = Field(
        ..., description='云平台创建或复刻后得到的稳定 voice id；未创建时保持空字符串。'
    )


class VoiceBindingsConfig(BaseModel):
    model_config = ConfigDict(
        extra='forbid',
        frozen=True,
    )
    dashscope_cosyvoice: CloudVoiceBinding = Field(..., alias='dashscope-cosyvoice')


class VoiceConfig(BaseModel):
    model_config = ConfigDict(
        extra='forbid',
        frozen=True,
    )
    profile_id: str = Field(..., min_length=1)
    language: str = Field(..., min_length=2)
    style_instruction: str | None = ''
    prosody: VoiceProsodyConfig
    bindings: VoiceBindingsConfig
