# generated by datamodel-codegen:
#   filename:  ObservabilityConfig.schema.json

from __future__ import annotations

from enum import StrEnum

from pydantic import BaseModel, ConfigDict, Field


class ConsoleFormat(StrEnum):
    PRETTY = 'pretty'
    JSON = 'json'


class FileFormat(StrEnum):
    JSON = 'json'


class Level(StrEnum):
    DEBUG = 'debug'
    INFO = 'info'
    WARN = 'warn'
    ERROR = 'error'


class ModuleLevels(StrEnum):
    DEBUG = 'debug'
    INFO = 'info'
    WARN = 'warn'
    ERROR = 'error'


class LogRotationConfig(BaseModel):
    model_config = ConfigDict(
        extra='forbid',
        frozen=True,
    )
    main_size_mb: int | None = Field(
        10, description='主日志单文件大小上限（MB）。', ge=1
    )
    main_keep: int | None = Field(5, description='主日志保留文件数。', ge=1)
    error_size_mb: int | None = Field(
        5, description='错误日志单文件大小上限（MB）。', ge=1
    )
    error_keep: int | None = Field(3, description='错误日志保留文件数。', ge=1)


class CaptureMode(StrEnum):
    OFF = 'off'
    SUMMARY = 'summary'
    FULL = 'full'


class ModelInvocationCaptureConfig(BaseModel):
    model_config = ConfigDict(
        extra='forbid',
        frozen=True,
    )
    capture_mode: CaptureMode | None = Field(
        'summary',
        description='采集模式：off 不记录；summary 仅摘要；full 落完整 capture。',
    )
    full_retention_days: int | None = Field(
        3, description='full 模式 capture 的建议保留天数。', ge=1
    )
    redact_secrets: bool | None = Field(
        True, description='是否在记录与 capture 中脱敏 API key、Bearer token 等敏感值。'
    )


class ObservabilityRetentionConfig(BaseModel):
    model_config = ConfigDict(
        extra='forbid',
        frozen=True,
    )
    events_days: int | None = Field(14, ge=1)
    traces_days: int | None = Field(14, ge=1)
    metrics_days: int | None = Field(14, ge=1)
    audit_days: int | None = Field(30, ge=1)
    model_invocation_days: int | None = Field(14, ge=1)
    application_log_days: int | None = Field(7, ge=1)
    dlq_days: int | None = Field(30, ge=1)
    bundles_days: int | None = Field(7, ge=1)


class Mode(StrEnum):
    SQLITE = 'sqlite'
    JSONL_SCAN = 'jsonl_scan'


class ObservabilityIndexConfig(BaseModel):
    model_config = ConfigDict(
        extra='forbid',
        frozen=True,
    )
    mode: Mode | None = 'sqlite'
    db_path: str | None = 'observability/index/observability.db'


class ObservabilityBundleConfig(BaseModel):
    model_config = ConfigDict(
        extra='forbid',
        frozen=True,
    )
    export_dir: str | None = 'observability/bundles'
    process_tail_bytes: int | None = Field(8192, ge=512)
    include_model_invocation_captures: bool | None = False


class ObservabilityConfig(BaseModel):
    model_config = ConfigDict(
        extra='forbid',
        frozen=True,
    )
    console_format: ConsoleFormat | None = Field(
        'pretty', description='控制台输出格式：pretty 供开发阅读；json 供机器解析。'
    )
    file_format: FileFormat | None = Field(
        'json', description='文件输出格式，固定为 json。'
    )
    level: Level | None = Field('info', description='全局默认日志级别。')
    module_levels: dict[str, ModuleLevels] | None = Field(
        {}, description='按模块覆盖日志级别。'
    )
    rotation: LogRotationConfig | None = Field({}, validate_default=True)
    model_invocations: ModelInvocationCaptureConfig | None = Field(
        {}, validate_default=True
    )
    retention: ObservabilityRetentionConfig | None = Field({}, validate_default=True)
    index: ObservabilityIndexConfig | None = Field({}, validate_default=True)
    bundles: ObservabilityBundleConfig | None = Field({}, validate_default=True)
