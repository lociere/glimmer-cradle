# generated by datamodel-codegen:
#   filename:  IPCConfig.schema.json

from __future__ import annotations

from typing import Literal

from pydantic import BaseModel, ConfigDict, Field


class IPCConfig(BaseModel):
    model_config = ConfigDict(
        extra='forbid',
        frozen=True,
    )
    bind_address: Literal['tcp://127.0.0.1:*'] = Field(
        'tcp://127.0.0.1:*',
        description='ZMQ 回环绑定策略；实际端口由操作系统分配并由 EndpointRegistry 发布',
    )
    request_timeout_ms: int | None = Field(
        30000, description='单次 IPC 请求超时（ms）', ge=1000
    )
    retry_count: int | None = Field(2, description='请求失败重试次数', ge=0)
    retry_interval_ms: int | None = Field(2000, description='重试间隔（ms）', ge=0)
    heartbeat_interval_ms: int | None = Field(
        5000, description='Python 层心跳检测间隔（ms）', ge=1000
    )
