# generated by datamodel-codegen:
#   filename:  LLMConfig.schema.json

from __future__ import annotations

from enum import StrEnum

from pydantic import BaseModel, ConfigDict, Field


class DefaultRoute(BaseModel):
    model_config = ConfigDict(
        extra='forbid',
        frozen=True,
    )
    provider: str | None = Field(None, description='默认对话路由选定的 provider key。')
    model_alias: str | None = Field(None, description='默认对话路由选定的模型别名。')


class RequestMethod(StrEnum):
    GET = 'GET'
    POST = 'POST'
    PUT = 'PUT'
    PATCH = 'PATCH'
    DELETE = 'DELETE'


class LLMProviderConfig(BaseModel):
    model_config = ConfigDict(
        extra='forbid',
        frozen=True,
    )
    api_type: str | None = Field('openai', description='API 协议类型')
    api_key: str | None = Field(
        None, description='API Key（优先从 secrets.yaml 注入，此处可留空）'
    )
    base_url: str | None = Field(None, description='API 端点地址')
    models: dict[str, str] = Field(
        ..., description='模型字典：alias → 模型 ID；无 alias 时取第一个值'
    )
    temperature: float | None = Field(None, description='采样温度覆盖', ge=0.0, le=2.0)
    request_method: RequestMethod | None = Field(None, description='自定义 HTTP 方法')
    request_path: str | None = Field(None, description='自定义请求路径')
    request_headers: dict[str, str] | None = Field(None, description='自定义请求头')
    request_body_template: str | None = Field(
        None, description='请求体模板（支持 {prompt} / {model} / {temperature} 占位符）'
    )
    response_extract: str | None = Field(
        None, description='响应解析路径（点分隔，例如 choices.0.text）'
    )


class LLMConfig(BaseModel):
    model_config = ConfigDict(
        extra='forbid',
        frozen=True,
    )
    default_route: DefaultRoute | None = Field(
        None,
        description='显式默认对话路由。Kernel Config Application Port 负责把它投影为根配置。',
    )
    api_type: str | None = Field('openai', description='主 API 协议类型')
    api_key: str | None = Field(
        None, description='主 API Key（优先从 secrets.yaml 注入）'
    )
    base_url: str | None = Field(None, description='主 API 端点')
    models: dict[str, str] | None = Field(
        None,
        description='根配置模型字典：alias → 模型 ID；provider_key=None 时取第一个值',
    )
    temperature: float | None = Field(None, description='主采样温度', ge=0.0, le=2.0)
    providers: dict[str, LLMProviderConfig] | None = Field(
        None, description='多提供商配置映射（key = 提供商名称）'
    )
    request_method: RequestMethod | None = Field(None, description='自定义 HTTP 方法')
    request_path: str | None = Field(None, description='自定义请求路径')
    request_headers: dict[str, str] | None = Field(None, description='自定义请求头')
    request_body_template: str | None = Field(None, description='请求体模板')
    response_extract: str | None = Field(None, description='响应解析路径')
