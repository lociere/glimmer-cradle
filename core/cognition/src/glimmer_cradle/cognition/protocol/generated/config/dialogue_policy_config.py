# generated by datamodel-codegen:
#   filename:  DialoguePolicyConfig.schema.json

from __future__ import annotations

from pydantic import BaseModel, ConfigDict, Field, RootModel


class Rule(RootModel[str]):
    root: str = Field(..., min_length=1)


class Presentation(BaseModel):
    model_config = ConfigDict(
        extra='forbid',
        frozen=True,
    )
    forbid_stage_directions: bool | None = True
    forbid_emotion_labels: bool | None = True
    casual_max_sentences: int | None = Field(3, ge=1, le=8)
    casual_max_chars_per_message: int | None = Field(48, ge=12, le=160)
    complex_reply_policy: str | None = Field(
        '复杂问题先给短结论，再按必要结构展开。', min_length=1
    )
    message_split_policy: str | None = Field(
        '普通闲聊优先 1 到 3 个短句；信息密度高时自然分段。', min_length=1
    )
    rules: list[Rule] = Field(..., min_length=1)


class StructuredOutput(BaseModel):
    model_config = ConfigDict(
        extra='forbid',
        frozen=True,
    )
    preserve_markdown: bool | None = True
    preserve_code_blocks: bool | None = True
    require_fenced_code_blocks: bool | None = True
    rules: list[Rule] = Field(..., min_length=1)


class Normalization(BaseModel):
    model_config = ConfigDict(
        extra='forbid',
        frozen=True,
    )
    strip_stage_directions: bool | None = True
    strip_emotion_labels: bool | None = True


class DialoguePolicyConfig(BaseModel):
    model_config = ConfigDict(
        extra='forbid',
        frozen=True,
    )
    presentation: Presentation
    structured_output: StructuredOutput
    normalization: Normalization | None = Field({}, validate_default=True)
