# generated by datamodel-codegen:
#   filename:  KnowledgeIndexConfig.schema.json

from __future__ import annotations

from enum import StrEnum

from pydantic import BaseModel, ConfigDict, Field


class KnowledgeIndexEntry(BaseModel):
    model_config = ConfigDict(
        extra='forbid',
        frozen=True,
    )
    entry_id: str = Field(..., min_length=1)
    file: str = Field(..., min_length=1)
    priority: int = Field(..., ge=1, le=100)
    enabled: bool | None = True


class Mode(StrEnum):
    FULL_INJECTION = 'full_injection'
    SEMANTIC_RAG = 'semantic_rag'


class KnowledgeIndexRetrievalConfig(BaseModel):
    model_config = ConfigDict(
        extra='forbid',
        frozen=True,
    )
    mode: Mode | None = 'full_injection'
    top_k: int | None = Field(5, ge=1, le=50)
    min_score: float | None = Field(0.3, ge=0.0, le=1.0)
    semantic_weight: float | None = Field(0.6, ge=0.0, le=1.0)


class KnowledgeIndexConfig(BaseModel):
    model_config = ConfigDict(
        extra='forbid',
        frozen=True,
    )
    version: str = Field(..., min_length=1)
    retrieval: KnowledgeIndexRetrievalConfig
    entries: list[KnowledgeIndexEntry]
