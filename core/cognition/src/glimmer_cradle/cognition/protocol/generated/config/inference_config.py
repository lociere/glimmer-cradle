# generated by datamodel-codegen:
#   filename:  InferenceConfig.schema.json

from __future__ import annotations

from enum import StrEnum

from pydantic import BaseModel, ConfigDict, Field


class ModelConfig(BaseModel):
    model_config = ConfigDict(
        extra='forbid',
        frozen=True,
    )
    max_tokens: int | None = Field(1024, description='单次生成最大 token 数', ge=1)
    temperature: float | None = Field(
        0.8, description='采样温度（0 = 确定性，2 = 最大随机）', ge=0.0, le=2.0
    )
    top_p: float | None = Field(0.9, description='核采样概率阈值', ge=0.0, le=1.0)
    frequency_penalty: float | None = Field(
        0.0, description='频率惩罚因子', ge=-2.0, le=2.0
    )


class LifeClockConfig(BaseModel):
    model_config = ConfigDict(
        extra='forbid',
        frozen=True,
    )
    heartbeat_enabled: bool | None = Field(
        False, description='是否启用 Kernel 到 Cognition 的活性探测。'
    )
    heartbeat_interval_ms: int | None = Field(
        45000,
        description='活性探测的兜底间隔（ms）；收到 Cognition CognitiveActivityPolicy.frequency_hint_ms 后可调整探测频率。',
        ge=1000,
    )
    focus_duration_ms: int | None = Field(
        20000, description='焦点模式持续时长（ms）', ge=1000
    )
    ingress_debounce_ms: int | None = Field(
        1400, description='普通消息防抖时间窗口（ms）', ge=100
    )
    ingress_focused_debounce_ms: int | None = Field(
        700, description='焦点模式下的防抖时间窗口（ms）', ge=50
    )
    ingress_max_batch_messages: int | None = Field(
        4, description='单次批处理最大消息条数', ge=1
    )
    ingress_max_batch_items: int | None = Field(
        24, description='单次批处理最大媒体项数', ge=1
    )
    summon_keywords: list[str] | None = Field([], description='唤醒关键词列表')
    focus_on_any_chat: bool | None = Field(False, description='是否任意聊天都触发焦点')


class Strategy(StrEnum):
    CORE_DIRECT = 'core_direct'
    SPECIALIST_THEN_CORE = 'specialist_then_core'


class MultimodalConfig(BaseModel):
    model_config = ConfigDict(
        extra='forbid',
        frozen=True,
    )
    enabled: bool | None = Field(False, description='是否启用多模态输入处理')
    strategy: Strategy | None = Field(
        'specialist_then_core',
        description='处理策略：core_direct = 主模型直接处理；specialist_then_core = 专家预处理后传主模型',
    )
    max_items: int | None = Field(6, description='单次请求最大媒体项数', ge=1)
    core_model: str | None = Field('deepseek', description='主推理模型名称')
    image_model: str | None = Field('', description='图像专家模型名称')
    video_model: str | None = Field('', description='视频专家模型名称')


class Channel(StrEnum):
    LIVE2D = 'live2d'


class ActionStreamConfig(BaseModel):
    model_config = ConfigDict(
        extra='forbid',
        frozen=True,
    )
    enabled: bool | None = Field(False, description='是否启用动作流')
    channel: Channel | None = Field('live2d', description='渲染通道')


class InferenceConfig(BaseModel):
    model_config = ConfigDict(
        extra='forbid',
        frozen=True,
    )
    model: ModelConfig
    life_clock: LifeClockConfig
    multimodal: MultimodalConfig
    action_stream: ActionStreamConfig
