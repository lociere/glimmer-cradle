# generated by datamodel-codegen:
#   filename:  CharacterManifestConfig.schema.json

from __future__ import annotations

from enum import StrEnum

from pydantic import BaseModel, ConfigDict, Field


class PersonaMode(StrEnum):
    API = 'api'
    LOCAL_BASE = 'local_base'
    LOCAL_FINETUNE = 'local_finetune'


class BaseCharacter(BaseModel):
    model_config = ConfigDict(
        extra='forbid',
        frozen=True,
    )
    name: str = Field(..., description='角色正式名（英文 / 拼音）', min_length=1)
    nickname: str = Field(..., description='角色昵称 / 中文名', min_length=1)


class CharacterAssets(BaseModel):
    model_config = ConfigDict(
        extra='forbid',
        frozen=True,
    )
    root: str | None = Field('assets', min_length=1)


class CharacterKnowledge(BaseModel):
    model_config = ConfigDict(
        extra='forbid',
        frozen=True,
    )
    index: str | None = Field('knowledge/index.yaml', min_length=1)


class CharacterMigrations(BaseModel):
    model_config = ConfigDict(
        extra='forbid',
        frozen=True,
    )
    root: str | None = Field('migrations', min_length=1)


class CharacterManifestConfig(BaseModel):
    model_config = ConfigDict(
        extra='forbid',
        frozen=True,
    )
    character_id: str = Field(..., min_length=1)
    base: BaseCharacter
    persona_mode: PersonaMode = Field(
        ...,
        description='人格模式：api/local_base 由 profile.yaml 与 dialogue.yaml 提供风格和呈现策略；local_finetune 表示主要风格已烘焙进模型权重，运行时保留最小身份、安全边界与必要对话策略。',
    )
    assets: CharacterAssets
    knowledge: CharacterKnowledge
    migrations: CharacterMigrations
