# generated by datamodel-codegen:
#   filename:  WorkspaceItem.schema.json

from __future__ import annotations

from enum import StrEnum
from typing import Any

from pydantic import BaseModel, ConfigDict, Field


class Source(StrEnum):
    PERCEPTION = 'perception'
    AFFECT = 'affect'
    MEMORY = 'memory'
    DRIVE = 'drive'
    SOCIAL = 'social'


class WorkspaceItem(BaseModel):
    model_config = ConfigDict(
        extra='forbid',
    )
    item_id: str = Field(..., description='工作区项唯一 ID（UUIDv4 hex，无连字符）')
    source: Source = Field(
        ...,
        description='投放此项的专家模块（与 Cognition workspace provider 五件套对齐）',
    )
    content: dict[str, Any] = Field(
        ..., description='意义内容（按 source 解释，自由形态）'
    )
    salience: float = Field(
        ...,
        description='显著度 [0,1] —— 工作区竞争的排序依据，高者胜出',
        ge=0.0,
        le=1.0,
    )
    created_at: str = Field(..., description='投放时刻（UTC 毫秒 ISO8601）')
    decay_at: str | None = Field(
        None,
        description='衰减失效时刻（UTC 毫秒 ISO8601）；缺省表示由工作区按容量策略淘汰',
    )
