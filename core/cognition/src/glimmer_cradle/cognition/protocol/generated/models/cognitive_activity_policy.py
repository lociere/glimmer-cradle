# generated by datamodel-codegen:
#   filename:  CognitiveActivityPolicy.schema.json

from __future__ import annotations

from enum import StrEnum

from pydantic import BaseModel, ConfigDict, Field


class ModelTier(StrEnum):
    NONE = 'none'
    LOCAL_ONLY = 'local_only'
    CLOUD_ALLOWED = 'cloud_allowed'


class CognitiveActivityPolicy(BaseModel):
    model_config = ConfigDict(
        extra='forbid',
    )
    frequency_hint_ms: int = Field(
        ...,
        description='认知循环频率建议（毫秒）—— TS LifeClock 心跳间隔的来源。quiescent=60000、ambient=45000、engaged=10000。',
        ge=0,
    )
    allows_proactive: bool = Field(
        ...,
        description='是否允许 Volition 主动开口。quiescent=false；ambient=true（受限）；engaged=true。',
    )
    model_tier: ModelTier = Field(
        ...,
        description='ReasoningService 的推理访问策略。none=禁止推理；local_only=只允许已接入的本地后端；cloud_allowed=允许已配置的远端 provider，并可降级到真实本地后端。',
    )
    context_budget_factor: float = Field(
        ...,
        description='ContextAssembly 的上下文预算缩放因子。quiescent=0.0、ambient=0.6、engaged=1.0。',
        ge=0.0,
        le=1.0,
    )
